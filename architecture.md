# โครงสร้างการทำงานของระบบ (Architecture)
ระบบจะถูกแบ่งออกเป็น 2 ฝั่งหลักๆ คือ Host (เครื่องที่เราจะรีโมทไปหา) และ Client (เครื่องที่เราใช้ควบคุม)



## ฝั่ง Host (Windows เครื่องเป้าหมาย)

- Loop จับภาพ: ใช้ dxcam ดึงภาพหน้าจอปัจจุบันออกมาเป็น NumPy Array

- Compress: ใช้ PyTurboJPEG แปลงภาพเป็น Byte จังหวะนี้จะช่วยลดขนาดข้อมูลลงได้มหาศาล

- Send: ส่ง Byte ภาพผ่าน WebSockets ไปยัง Client

- Listen Input: รอรับพิกัดเมาส์ (X, Y) หรือปุ่มคีย์บอร์ดที่ส่งมาจาก Client แล้วใช้ pydirectinput สั่งงานเครื่อง Windows


## ฝั่ง Client (เครื่องควบคุม - จะเป็น OS อะไรก็ได้)

- UI Display: สร้างหน้าต่างด้วย PyQt6, Tkinter หรือจะทำเป็น Web App (HTML5 Canvas) เพื่อแสดงภาพที่ส่งมา

- Listen Event: คอยดักจับ Event การขยับเมาส์ การคลิก หรือการกดปุ่มบนหน้าต่าง UI นั้น

- Send Input: แปลงพิกัดเมาส์ให้สัมพันธ์กับขนาดจอของ Host แล้วส่งกลับไปทาง WebSockets

